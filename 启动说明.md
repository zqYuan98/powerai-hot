# 电力 AI-hot 启动说明

本说明用于 Windows 本地开发。默认使用 SQLite，不要求 PostgreSQL、Redis 或 Docker。

## 端口

| 服务 | 地址 |
|---|---|
| 后端 API | <http://127.0.0.1:8010> |
| API 文档（仅本地 DEBUG） | <http://127.0.0.1:8010/docs> |
| 前端 | <http://127.0.0.1:3010> |

## 一键启动

在 `powerai-hot` 目录打开 PowerShell：

```powershell
powershell -ExecutionPolicy Bypass -File scripts\run-backend.ps1
```

另开一个 PowerShell：

```powershell
powershell -ExecutionPolicy Bypass -File scripts\run-frontend.ps1
```

浏览器打开 <http://127.0.0.1:3010>。

## 手动启动

后端：

```powershell
cd backend
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements-local.txt
Copy-Item .env.example .env
python -m models.seed
python -m uvicorn main:app --reload --port 8010
```

前端（新终端）：

```powershell
cd frontend
npm.cmd install
$env:BACKEND_URL = "http://127.0.0.1:8010"
npm.cmd run dev -- -p 3010
```

`python -m models.seed` 会建表并登记基础信源、知识库参考和订阅关键词，不会写入演示情报。页面数据来自后端数据库，后端不可用时不会回退到前端假数据。

## 功能入口

当前前端包含：情报信息流、今日精选、我的知识库、研报、业务知识库、知识关系图谱、推送订阅、信源提报、流水线、信源管理和管理后台。

真实采集器包括北极星电力网、RSS、arXiv 和 Hugging Face Papers。自动采集会产生外部请求，配置模型 Key 后还可能产生调用费用；仅浏览界面时可在 `.env` 设置：

```dotenv
AUTO_CRAWL_ENABLED=false
```

## 常见问题

- 端口被占用：先确认是否已有后端/前端实例，不要重复启动。也可同时修改启动端口和前端 `BACKEND_URL`。
- 页面无数据：确认后端可访问、已执行 seed，并在流水线/信源管理中触发真实采集。
- AI 分析无结果：检查 `DEEPSEEK_API_KEY` 和模型配置；切换自定义模型时需同时配置 `CUSTOM_API_URL` 和 `CUSTOM_MODEL`（`CUSTOM_API_KEY` 私有化部署可留空）。
- Docker 部署：先阅读 [部署指南](DEPLOY.md)，尤其要避免 APScheduler 与 Celery beat 同时运行。
